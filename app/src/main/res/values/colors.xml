<resources>
    <!-- Colours live in the Compose theme (ui/theme/Color.kt). This file is kept
         because AGP's default template references it; add resource colours here
         only for things Compose cannot reach, e.g. an XML theme or a widget. -->
</resources>
